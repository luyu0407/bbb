<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConstraintActivity"></activity>
        <activity
            android:name=".WebViewActivity"
            android:windowSoftInputMode="adjustResize"></activity>
        <activity android:name=".NDKActivity" />
        <activity android:name=".HorizontalActivity" />
        <activity android:name=".LiuhaiActivity" />
        <activity android:name=".colorfiter.ColorFiterActivity" />
        <activity android:name=".eventbus.EB2Activity" />
        <activity android:name=".eventbus.EBActivity" />
        <activity android:name=".music.MusicListActivity" />

        <service
            android:name=".MyService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".ImmersiveActivity" />
        <activity android:name=".CustomViewActivity" />
        <activity android:name=".AnimationActivity" />
        <activity android:name=".SecondAcitvity" />
        <activity android:name=".PathMeasureActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".service.MyIntentService" />
    </application>

</manifest>